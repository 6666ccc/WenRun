# 温润在线医院 · 项目流程骨架

以仓库当前代码为准。浏览器只访问 Java 的 `/api/**`，不直连 Python `:8000`。

## 1. 系统分层

```text
患者浏览器  Vue :5173
    │  Bearer Token（登录 Session）
    ▼
Java  Spring Boot :8080
    │  业务写路径 → MySQL
    │  登录 Session → Redis db1（可降级内存）
    │  AI 转发 → X-Api-Key + X-Delegated-Token + X-Request-Id
    ▼
Python  FastAPI + LangGraph :8000
    │  知识检索 → Qdrant
    │  会话 checkpoint → Redis db0（未配 AI_REDIS_URL 则无记忆）
    │  模型 / Embedding → DashScope
    └─ 业务只读 Tool → Java /api/internal/ai-tools/** → MySQL
```

职责切分：

| 层 | 负责 | 不负责 |
| --- | --- | --- |
| Vue | 患者门户、SSE 展示、跨页保活 | 不持有服务间密钥，不直连 Python |
| Java | 登录、会话归属、HIS 写操作、AI 网关、委托 JWT | 不跑模型、不查 Qdrant |
| Python | 意图路由、RAG、闲聊、只读 Tool 编排、SSE 生成 | 不直连 MySQL，不执行挂号写操作 |

## 2. 通用请求骨架

除登录、注册、健康检查外，业务请求都走同一条鉴权链：

```text
请求
  → AuthInterceptor（Authorization: Bearer 或 X-Token）
  → TokenSessionStore 取 userId / accountType
  → UserContext
  → Controller → Service → MySQL
  → 清除 UserContext
```

三把钥匙互不混用：

1. **登录 Token**：浏览器 → Java，证明「这个人已登录」。
2. **X-Api-Key**：Java → Python，证明「这是内部 AI 服务调用」。
3. **委托 JWT**：Python → Java `/api/internal/ai-tools`，证明「这次只能用这些只读 scope，且患者身份钉死在令牌里」。

## 3. 患者门户骨架

当前 Vue 路由只覆盖患者端：

```text
/login
  → 登录 / 注册  POST /api/auth/login | /register
  → 首页 /home
       ├─ 预约挂号     /registration  →  /registration/:id
       ├─ 健康助手     /assistant
       ├─ 门诊缴费     /payment       →  /payment/:id
       ├─ 科室医生     /department
       └─ 我的档案     /user
```

未登录访问业务页会回到 `/login`；已登录访问 `/login` 会进首页。

## 4. HIS 主链路骨架（写操作在 Java）

患者端实际能走完的就诊闭环：

```text
浏览科室 / 医生 / 排班
  GET /api/depts | /api/staff | /api/schedules
        │
        ▼
挂号（锁号源）
  POST /api/registrations
        │
        ├─ 退号  POST /api/registrations/{id}/cancel
        │
        ▼
就诊（Java API 已有，当前 Vue 无医护工作台）
  POST /api/visits/start/{registrationId}
        │
        ▼
收费单
  POST /api/charges/from-visit/{visitId}
        │
        ▼
缴费
  POST /api/charges/{id}/pay
```

说明：

- **挂号、退号、缴费**由患者前端直接打 Java 业务 API，不经过 LangGraph。
- Java 另有处方、发药、检查、药品、库存、仪表盘等接口；当前前端没有对应路由，属于后台/医护能力骨架，未挂到患者门户。
- AI 的 Tool 只镜像了科室、医生、排班、本人挂号的**只读查询**，没有 `register` / `cancel` / `pay`。

## 5. AI 对话骨架

### 5.1 进出站

```text
前端 useAssistant
  POST /api/ai/chat/stream
    { message, conversationId, clientRequestId, memoryEnabled, fastMode }
        │
        ▼
Java aiController
  1. 登录态 + 会话归属
  2. 幂等（同一 clientRequestId）
  3. 先写用户消息到 MySQL
  4. 签发只读委托 JWT
  5. 转发 Python /v1/chat/stream
        │
        ▼
Python LangGraph astream
  SSE：status → token → citation → done | error
        │
        ▼
Java 收到 done 后写助手消息
前端以 done.reply 为最终正文
```

删除会话：

```text
DELETE /api/ai/conversations/{conversationId}
  → 校验归属
  → 删 MySQL chat_messages
  → DELETE /v1/chat/memory/{conversationId} 清 Redis checkpoint
```

### 5.2 图内顺序（固定边，节点内跳过）

```text
START
  → begin_node          意图多选 knowledge / chat / tools，清空上一轮回复字段
  → knowledge_node      院内 Qdrant；未命中再 Tavily
  → chat_node           闲聊 / 院务引导
  → tool_node           只读业务查询（需委托 JWT）
  → final_node          单回复原样输出，多回复才合并
  → summarize_node      messages > 12 才压缩摘要
  → END
```

`fastMode=true` 时不走上面这张图，改用快速模式图：

```text
START
  → fast_node           单个全能 Agent，自带工具循环：院内 Qdrant 检索 + Tavily 联网
  → summarize_node      messages > 12 才压缩摘要
  → END
```

快速模式只挂 `search_hospital_knowledge` 与 `web_search` 两个工具，没有业务只读工具，问号源、排班、我的预约时会引导患者关闭快速模式。工具循环在 `fast_node` 内部完成，中间的 tool_calls 与 ToolMessage 不写回 State，因此两张图的 checkpoint 结构一致，共用 `conversationId` 作为 `thread_id`，记忆在两种模式之间连续。

未写入 `selected_agents` 的节点直接返回空 dict。

SSE token 只取根图节点的模型分片，子图命名空间（`ns` 非空）的嵌套 Agent 分片一律丢弃，避免泄漏路由 JSON 和工具原文。单意图且该节点在根图直接产出正文时，token 就来自它本身：纯闲聊来自 `chat_node`，纯知识来自 `knowledge_node`（仅 RAG 命中分支）。多意图时 `final_node` 会重写正文，只流出 `final_node`。`tool_node` 与知识的 Tavily 兜底由嵌套 Agent 生成，不流式，等 `final_node` 透传后一次性下发。快速模式下 token 只来自 `fast_node`。

### 5.3 记忆开关

```text
启动
  AI_REDIS_URL 可用？
    是 → AsyncShallowRedisSaver + 带 checkpointer 的图
    否 → 退化为模块级无状态图（每轮只有当前这一句）
请求
  memoryEnabled=false 或 memory_graph 为空 → 仍走无状态图
  否则 thread_id = conversationId，最近 6 条消息 + summary
```

委托令牌只活在 `HospitalToolContext`，不进 State，因此不会写入 checkpoint。

## 6. 总图

```mermaid
flowchart TB
    User[患者]
    Vue[Vue 患者门户]
    User --> Vue

    subgraph Java[Java :8080 业务边界]
        Auth[AuthController]
        Session[TokenSessionStore Redis db1]
        Gate[AuthInterceptor]
        HIS[科室 / 排班 / 挂号 / 收费]
        AiGw[aiController]
        Own[会话归属 + 幂等]
        Jwt[只读委托 JWT]
        Tools[AiToolController 只读]
    end

    subgraph Py[Python :8000 AI 编排]
        Stream["/v1/chat/stream"]
        G[LangGraph 固定顺序图]
        Begin[begin_node]
        Know[knowledge_node]
        Chat[chat_node]
        ToolN[tool_node]
        Final[final_node]
        Sum[summarize_node]
    end

    subgraph Data[数据]
        MySQL[(MySQL)]
        Qdrant[(Qdrant)]
        RedisMem[(Redis db1 checkpoint)]
        LLM[DashScope]
        Web[Tavily]
    end

    Vue -->|"登录"| Auth
    Auth --> Session
    Auth --> MySQL
    Vue -->|"HIS 写/读 + Bearer"| Gate --> HIS --> MySQL
    Vue -->|"聊天 SSE + Bearer"| Gate --> AiGw
    AiGw --> Own --> MySQL
    AiGw --> Jwt
    AiGw -->|"X-Api-Key + 委托 JWT"| Stream --> G
    G --> Begin --> Know --> Chat --> ToolN --> Final --> Sum
    Know --> Qdrant
    Know --> Web
    Know --> LLM
    Chat --> LLM
    ToolN -->|"Bearer 委托 JWT"| Tools --> MySQL
    Sum --> RedisMem
    G -->|"SSE"| AiGw --> Vue
    AiGw -->|"done 后写助手消息"| MySQL
```

## 7. 和旧文档的差异

[`求职项目评估与流程图.md`](求职项目评估与流程图.md) 第 6 节、以及运维指南第 4 节里曾经出现的 `hospital / medical / clarify / interrupt 挂号确认 / 知识库管理代理` 不是当前实现。当前图是 `begin → knowledge → chat → tool → final → summarize`；挂号写操作只在患者门户的 Java API 上。
