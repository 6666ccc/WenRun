<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.ExamRequestRepository">
    <select id="selectPendingByVisitId" resultType="com.wenrun.entity.ExamRequest">
        SELECT * FROM exam_request WHERE visit_id = #{visitId} AND status = #{status}
    </select>
    <select id="selectByVisitId" resultType="com.wenrun.entity.ExamRequest">
        SELECT * FROM exam_request WHERE visit_id = #{visitId}
    </select>
    <select id="selectById" resultType="com.wenrun.entity.ExamRequest">
        SELECT * FROM exam_request WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO exam_request (request_no, visit_id, patient_id, item_id, amount, status)
        VALUES (#{requestNo}, #{visitId}, #{patientId}, #{itemId}, #{amount}, #{status})
    </insert>
    <update id="updateStatus">
        UPDATE exam_request SET status = #{status} WHERE id = #{id}
    </update>
    <update id="updateStatusByVisitId">
        UPDATE exam_request SET status = #{toStatus}
        WHERE visit_id = #{visitId} AND status = #{fromStatus}
    </update>
</mapper>
