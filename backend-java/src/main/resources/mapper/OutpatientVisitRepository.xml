<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.OutpatientVisitRepository">
    <select id="selectList" resultType="com.wenrun.vo.VisitVO">
        SELECT v.id, v.visit_no AS visitNo, v.registration_id AS registrationId,
               r.reg_no AS regNo, v.patient_id AS patientId, p.name AS patientName,
               v.staff_id AS staffId, s.name AS staffName, v.visit_time AS visitTime,
               v.chief_complaint AS chiefComplaint, v.diagnosis, v.status
        FROM outpatient_visit v
        LEFT JOIN registration r ON r.id = v.registration_id
        LEFT JOIN patient p ON p.id = v.patient_id
        LEFT JOIN staff s ON s.id = v.staff_id
        <where>
            <if test="status != null">AND v.status = #{status}</if>
            <if test="staffId != null">AND v.staff_id = #{staffId}</if>
        </where>
        ORDER BY v.visit_time DESC
    </select>
    <select id="selectById" resultType="com.wenrun.entity.OutpatientVisit">
        SELECT * FROM outpatient_visit WHERE id = #{id}
    </select>
    <select id="selectVoById" resultType="com.wenrun.vo.VisitVO">
        SELECT v.id, v.visit_no AS visitNo, v.registration_id AS registrationId,
               r.reg_no AS regNo, v.patient_id AS patientId, p.name AS patientName,
               v.staff_id AS staffId, s.name AS staffName, v.visit_time AS visitTime,
               v.chief_complaint AS chiefComplaint, v.diagnosis, v.status
        FROM outpatient_visit v
        LEFT JOIN registration r ON r.id = v.registration_id
        LEFT JOIN patient p ON p.id = v.patient_id
        LEFT JOIN staff s ON s.id = v.staff_id
        WHERE v.id = #{id}
    </select>
    <select id="selectByRegistrationId" resultType="com.wenrun.entity.OutpatientVisit">
        SELECT * FROM outpatient_visit WHERE registration_id = #{registrationId} LIMIT 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO outpatient_visit (visit_no, registration_id, patient_id, staff_id, visit_time,
                                      chief_complaint, diagnosis, status)
        VALUES (#{visitNo}, #{registrationId}, #{patientId}, #{staffId}, #{visitTime},
                #{chiefComplaint}, #{diagnosis}, #{status})
    </insert>
    <update id="updateById">
        UPDATE outpatient_visit
        <set>
            <if test="chiefComplaint != null">chief_complaint = #{chiefComplaint},</if>
            <if test="diagnosis != null">diagnosis = #{diagnosis},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
