<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.PatientMedicalDocumentRepository">

    <select id="selectCatalog" resultType="com.wenrun.ai.vo.PatientDocumentCatalogItem">
        SELECT id, doc_type, title, occurred_at
        FROM patient_medical_document
        WHERE patient_id = #{patientId}
          AND is_deleted = 0
        ORDER BY occurred_at DESC, id DESC
        LIMIT #{limit}
    </select>
</mapper>
