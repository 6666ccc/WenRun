<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.StaffRepository">
    <select id="selectList" resultType="com.wenrun.vo.StaffVO">
        SELECT s.id, s.staff_no AS staffNo, s.name, s.dept_id AS deptId,
               d.dept_name AS deptName, s.title, s.user_id AS userId, s.status
        FROM staff s
        LEFT JOIN dept d ON d.id = s.dept_id
        <where>
            <if test="deptId != null">AND s.dept_id = #{deptId}</if>
            <if test="status != null">AND s.status = #{status}</if>
        </where>
        ORDER BY s.id
    </select>
    <select id="selectById" resultType="com.wenrun.entity.Staff">
        SELECT * FROM staff WHERE id = #{id}
    </select>
    <select id="selectByUserId" resultType="com.wenrun.entity.Staff">
        SELECT * FROM staff WHERE user_id = #{userId} LIMIT 1
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO staff (staff_no, name, dept_id, title, user_id, status)
        VALUES (#{staffNo}, #{name}, #{deptId}, #{title}, #{userId}, #{status})
    </insert>
    <update id="updateById">
        UPDATE staff
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="title != null">title = #{title},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
