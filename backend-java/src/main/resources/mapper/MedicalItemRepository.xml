<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.MedicalItemRepository">
    <select id="selectList" resultType="com.wenrun.entity.MedicalItem">
        SELECT * FROM medical_item
        <where>
            <if test="itemType != null">AND item_type = #{itemType}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY id
    </select>
    <select id="selectById" resultType="com.wenrun.entity.MedicalItem">
        SELECT * FROM medical_item WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO medical_item (item_code, item_name, item_type, price, dept_id, status)
        VALUES (#{itemCode}, #{itemName}, #{itemType}, #{price}, #{deptId}, #{status})
    </insert>
    <update id="updateById">
        UPDATE medical_item
        <set>
            <if test="itemName != null">item_name = #{itemName},</if>
            <if test="itemType != null">item_type = #{itemType},</if>
            <if test="price != null">price = #{price},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
