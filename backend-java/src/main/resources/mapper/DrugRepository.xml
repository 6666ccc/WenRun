<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.DrugRepository">
    <select id="selectList" resultType="com.wenrun.entity.Drug">
        SELECT * FROM drug
        <where>
            <if test="keyword != null and keyword != ''">
                AND (drug_name LIKE CONCAT('%', #{keyword}, '%')
                OR drug_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY id DESC
    </select>
    <select id="selectById" resultType="com.wenrun.entity.Drug">
        SELECT * FROM drug WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO drug (drug_code, drug_name, spec, unit, price, manufacturer, status)
        VALUES (#{drugCode}, #{drugName}, #{spec}, #{unit}, #{price}, #{manufacturer}, #{status})
    </insert>
    <update id="updateById">
        UPDATE drug
        <set>
            <if test="drugName != null">drug_name = #{drugName},</if>
            <if test="spec != null">spec = #{spec},</if>
            <if test="unit != null">unit = #{unit},</if>
            <if test="price != null">price = #{price},</if>
            <if test="manufacturer != null">manufacturer = #{manufacturer},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
