<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.ChargeOrderRepository">
    <select id="selectList" resultType="com.wenrun.vo.ChargeOrderVO">
        SELECT o.id, o.order_no AS orderNo, o.patient_id AS patientId, p.name AS patientName,
               o.visit_id AS visitId, o.total_amount AS totalAmount, o.paid_amount AS paidAmount,
               o.pay_type AS payType, o.pay_status AS payStatus, o.pay_time AS payTime, o.create_time AS createTime
        FROM charge_order o
        LEFT JOIN patient p ON p.id = o.patient_id
        <where>
            <if test="payStatus != null">AND o.pay_status = #{payStatus}</if>
            <if test="patientId != null">AND o.patient_id = #{patientId}</if>
        </where>
        ORDER BY o.create_time DESC
    </select>
    <select id="selectById" resultType="com.wenrun.entity.ChargeOrder">
        SELECT * FROM charge_order WHERE id = #{id}
    </select>
    <select id="selectVoById" resultType="com.wenrun.vo.ChargeOrderVO">
        SELECT o.id, o.order_no AS orderNo, o.patient_id AS patientId, p.name AS patientName,
               o.visit_id AS visitId, o.total_amount AS totalAmount, o.paid_amount AS paidAmount,
               o.pay_type AS payType, o.pay_status AS payStatus, o.pay_time AS payTime, o.create_time AS createTime
        FROM charge_order o LEFT JOIN patient p ON p.id = o.patient_id WHERE o.id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO charge_order (order_no, patient_id, visit_id, total_amount, paid_amount, pay_status, cashier_id)
        VALUES (#{orderNo}, #{patientId}, #{visitId}, #{totalAmount}, #{paidAmount}, #{payStatus}, #{cashierId})
    </insert>
    <update id="updatePay">
        UPDATE charge_order
        SET paid_amount = #{paidAmount}, pay_type = #{payType}, pay_status = #{payStatus},
            cashier_id = #{cashierId}, pay_time = #{payTime}
        WHERE id = #{id}
    </update>
</mapper>
