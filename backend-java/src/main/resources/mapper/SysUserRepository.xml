<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wenrun.repository.SysUserRepository">
    <select id="selectByUsername" resultType="com.wenrun.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username} LIMIT 1
    </select>
    <select id="selectByPhone" resultType="com.wenrun.entity.SysUser">
        SELECT * FROM sys_user WHERE phone = #{phone} LIMIT 1
    </select>
    <select id="selectById" resultType="com.wenrun.entity.SysUser">
        SELECT * FROM sys_user WHERE id = #{id} LIMIT 1
    </select>
    <select id="selectRolesByUserId" resultType="com.wenrun.entity.SysRole">
        SELECT r.* FROM sys_role r
        JOIN sys_user_role ur ON ur.role_id = r.id
        WHERE ur.user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, phone, account_type, status)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{accountType}, #{status})
    </insert>

    <insert id="insertUserRole">
        INSERT INTO sys_user_role (user_id, role_id) VALUES (#{userId}, #{roleId})
    </insert>

    <select id="selectRoleByCode" resultType="com.wenrun.entity.SysRole">
        SELECT * FROM sys_role WHERE role_code = #{roleCode} LIMIT 1
    </select>

    <update id="updateById">
        UPDATE sys_user
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>
