[project]
name = "wenrun-ai"
version = "0.1.0"
description = "WenRun FastAPI and LangGraph AI service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv",
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "loguru",
  "tavily-python",
  "langchain",
  "langchain-core",
  "langgraph",
  "langgraph-checkpoint-redis>=0.5.2,<0.6",
  "redis>=5.2.1",
  "langchain-openai",
  "langchain-qdrant",
  "langchain-text-splitters",
  "qdrant-client",
  "pypdf",
  "python-docx",
  "python-multipart",
  "PyJWT",
  "httpx",
]

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
