[project]
name = "wenrun-ai"
version = "0.1.0"
description = "WenRun FastAPI and LangGraph AI service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "python-dotenv",
  "fastapi",
  "uvicorn[standard]",
  "pydantic-settings",
  "loguru",
  "tavily-python",
  "langchain",
  "langchain-core",
  "langgraph",
  "langgraph-checkpoint-redis>=0.5.2,<0.6",
  "redis>=5.2.1",
  "langchain-openai",
  "langchain-qdrant",
  "langchain-text-splitters",
  "qdrant-client",
  "pypdf",
  "python-docx",
  "python-multipart",
  "PyJWT",
  "httpx",
  "pymysql>=1.1,<2",
  "scikit-learn>=1.5,<2",
]

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio", "ruff"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff.lint]
# FastAPI intentionally declares dependency providers and uploaded files in
# function defaults; B008 is not actionable for these route signatures.
ignore = ["B008"]
